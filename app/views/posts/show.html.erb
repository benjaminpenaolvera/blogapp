<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="show_post">
      <div class="header">
        <p class='lead' ><%= @post.created_at.strftime('%B %d, %Y') %></p>
        <h1><%= @post.title %></h1>
        <hr />
      </div>

      <div class="body">
        <div class="image">
          <%= image_tag(@post.header_url) if @post.header? %>
        </div>
        <div class="text">
          <%= @post.content %>
        </div>
      </div>
      <%= render "posts/partials/footer" %>
    </div>
  </div>
</div>
