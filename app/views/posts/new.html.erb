<h1>New POST</h1>

<%= form_for @post do |p| %>
  <%= p.label :title %>
  <%= p.text_field :title %>

  <%= p.label :content %>
  <%= p.text_area :content %>

  <%= p.label :tags %>
  <%= p.text_field :tags %>

  <%= p.button :submit %>
<% end %>
